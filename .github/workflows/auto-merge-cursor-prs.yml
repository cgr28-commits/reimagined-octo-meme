name: Auto-merge Cursor PRs

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, converted_to_draft]
  check_suite:
    types: [completed]
  status: {}

permissions:
  contents: write
  pull-requests: write
  checks: read
  actions: write

jobs:
  automerge:
    # Always auto-merge Cursor agent branches once checks pass (drafts are undrafted first).
    if: >
      (github.event_name == 'pull_request' && startsWith(github.head_ref, 'cursor/')) ||
      github.event_name == 'check_suite' ||
      github.event_name == 'status'
    runs-on: ubuntu-latest
    steps:
      - name: Merge when checks pass
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            // pull_request events often fire before Vercel/Workers finish — wait briefly.
            const maxAttempts = context.eventName === "pull_request" ? 24 : 3;
            const delayMs = 15000;

            async function sleep(ms) {
              await new Promise((resolve) => setTimeout(resolve, ms));
            }

            async function findCursorPulls() {
              const pulls = [];

              if (context.payload.pull_request?.head?.ref?.startsWith("cursor/")) {
                pulls.push(context.payload.pull_request);
                return pulls;
              }

              const headSha =
                context.payload.check_suite?.head_sha ||
                context.payload.sha ||
                null;

              if (headSha) {
                const { data } = await github.rest.pulls.list({
                  owner,
                  repo,
                  state: "open",
                  per_page: 50,
                });
                for (const pr of data) {
                  if (pr.head.ref.startsWith("cursor/") && pr.head.sha === headSha) {
                    pulls.push(pr);
                  }
                }
                return pulls;
              }

              return pulls;
            }

            async function tryMerge(pull_number) {
              let { data: prDetails } = await github.rest.pulls.get({
                owner,
                repo,
                pull_number,
              });

              if (prDetails.merged || !prDetails.head.ref.startsWith("cursor/")) {
                return "done";
              }

              if (prDetails.draft) {
                // Prefer leaving intentional drafts alone: undraft only when
                // the REST API successfully flips draft=false. If it remains
                // draft (common flake / permission), do not attempt merge.
                try {
                  await github.rest.pulls.update({
                    owner,
                    repo,
                    pull_number,
                    draft: false,
                  });
                } catch (error) {
                  console.warn(`Could not undraft PR #${pull_number}`, error);
                  return "wait";
                }
                ({ data: prDetails } = await github.rest.pulls.get({
                  owner,
                  repo,
                  pull_number,
                }));
                if (prDetails.draft) {
                  console.warn(
                    `PR #${pull_number} is still a draft after undraft attempt — skipping merge`,
                  );
                  return "wait";
                }
              }

              if (prDetails.mergeable === false) {
                return "blocked";
              }
              if (prDetails.mergeable !== true) {
                return "wait";
              }

              const { data: checks } = await github.rest.checks.listForRef({
                owner,
                repo,
                ref: prDetails.head.sha,
              });

              const ignored = new Set(["Auto-merge Cursor PRs", "automerge"]);
              const runs = checks.check_runs.filter((run) => !ignored.has(run.name));

              const blockingRuns = runs.filter(
                (run) =>
                  run.status !== "completed" ||
                  (run.conclusion &&
                    !["success", "skipped", "neutral", "stale"].includes(run.conclusion)),
              );
              if (blockingRuns.length > 0) {
                return "wait";
              }

              const { data: combined } = await github.rest.repos.getCombinedStatusForRef({
                owner,
                repo,
                ref: prDetails.head.sha,
              });
              if (combined.state === "pending" || combined.state === "failure") {
                return "wait";
              }

              try {
                await github.rest.pulls.merge({
                  owner,
                  repo,
                  pull_number,
                  merge_method: "merge",
                });
              } catch (error) {
                const status = error?.status || error?.response?.status;
                const message = String(error?.message || error || "");
                if (status === 405 && /draft/i.test(message)) {
                  console.warn(`PR #${pull_number} still draft at merge — skipping`);
                  return "wait";
                }
                throw error;
              }

              // GITHUB_TOKEN merges do not trigger push workflows — dispatch deploys.
              for (const workflow_id of ["deploy-pages.yml", "deploy-worker.yml"]) {
                try {
                  await github.rest.actions.createWorkflowDispatch({
                    owner,
                    repo,
                    workflow_id,
                    ref: "main",
                  });
                } catch (error) {
                  console.warn(`Failed to dispatch ${workflow_id}`, error);
                }
              }

              return "merged";
            }

            const candidates = await findCursorPulls();
            if (candidates.length === 0) {
              return;
            }

            for (const candidate of candidates) {
              for (let attempt = 1; attempt <= maxAttempts; attempt++) {
                const result = await tryMerge(candidate.number);
                if (result === "merged" || result === "done" || result === "blocked") {
                  break;
                }
                if (attempt < maxAttempts) {
                  await sleep(delayMs);
                }
              }
            }
