"use client";

/**
 * Legacy floating WhatsApp FAB — removed.
 * Mobile WhatsApp now lives in the Header quick-services row beside Get a Quote
 * (see Header.tsx `data-matni-whatsapp-quick`). Desktop still uses QuoteAssistant “?”.
 */
export default function WhatsAppButton() {
  return null;
}
