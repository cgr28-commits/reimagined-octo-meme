"use client";

/**
 * Floating WhatsApp FAB retired from the public homepage / Get a Quote funnel.
 * WhatsApp remains available via menu, footer, contact, and inline “Need help?” links.
 * This component stays mounted from the root layout as a no-op so portal gating
 * callers and offline checks can still import the module safely.
 */
export default function WhatsAppButton() {
  return null;
}
